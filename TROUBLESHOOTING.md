# Troubleshooting

Tracker 프로젝트를 개발하면서 겪은 주요 문제와 해결 과정을 정리한 문서입니다.

## 목차

1. [MyBatis INSERT문 STATUS 컬럼 매핑 누락](#1-mybatis-insert문-status-컬럼-매핑-누락)
2. [Dashboard 통계 기준 DUE_DATE 통일](#2-dashboard-통계-기준-due_date-통일)
3. [마감 준수율 0%, 평균 완료 소요일 0.0일 버그](#3-마감-준수율-0-평균-완료-소요일-00일-버그)

---

## 1. MyBatis INSERT문 STATUS 컬럼 매핑 누락

### 문제

새로운 작업을 생성할 때 서비스 계층에서 어떤 상태값을 설정하든 실제로는 항상 기본값(`TASK_STATUS = 'TODO'`, `STATUS = 'ACTIVE'`)으로만 저장되는 문제가 있었습니다.

### 원인

초기 CRUD 구현 당시에는 작업의 기본 정보(제목, 설명, 마감일 등) 위주로 INSERT 로직을 작성했고, 이후 상태 관리 기능을 추가하는 과정에서 `STATUS`, `TASK_STATUS` 두 컬럼이 INSERT SQL에 반영되지 않았습니다. DB 컬럼 자체에는 `DEFAULT` 값(`STATUS DEFAULT 'ACTIVE'`, `TASK_STATUS DEFAULT 'TODO'`, 둘 다 `NOT NULL`)이 걸려 있었기 때문에, 서비스 계층에서 세팅한 값이 SQL에서 아예 무시되고 항상 DEFAULT 값으로만 저장되고 있었습니다. 즉 값이 `NULL`로 비는 문제가 아니라, INSERT SQL과 서비스 로직 간 매핑 자체가 누락된 문제였습니다.

### 해결

INSERT문에 `STATUS`, `TASK_STATUS` 두 컬럼과 `#{status}`, `#{taskStatus}` 바인딩을 추가하여, 서비스 계층에서 설정한 상태값이 실제로 DB에 반영되도록 수정했습니다.

### 결과

작업 생성 시 의도한 상태값이 정상적으로 저장되도록 수정되었습니다.

### 설계 포인트

`STATUS`(논리 삭제 관리: `ACTIVE`/`DELETED`)와 `TASK_STATUS`(업무 진행 상태 관리: `TODO`/`DOING`/`DONE`)를 역할별로 분리 설계하여, 삭제된 데이터를 물리적으로 제거하지 않고도 상태별 조회·통계를 안정적으로 구현할 수 있었습니다. 또한 기능을 확장하는 과정에서 DB 테이블 구조와 MyBatis SQL 매핑 간 불일치가 생길 수 있다는 점을 직접 확인한 사례였습니다.

---

## 2. Dashboard 통계 기준 DUE_DATE 통일

### 문제

Dashboard/Reports의 통계가 `CREATED_DATE`(생성일) 기준으로 집계되고 있어 실제 업무 관리 목적과 맞지 않았습니다. 예를 들어 한 달 전에 등록됐지만 마감일이 오늘인 작업이 '오늘 할 일' 통계에 잡히지 않는 등, 사용자가 실제로 궁금해하는 "마감 기준 현황"과 화면에 보이는 통계가 어긋났습니다.

### 원인

초기 CRUD 구현 시 데이터 생성일 기준으로 조회/통계 로직을 설계했고, 이후 Dashboard·Reports 기능을 추가할 때도 기존 집계 기준을 그대로 재사용했기 때문입니다.

### 해결

총 작업 수, 완료 작업 수, 상태별 통계, 카테고리별 통계, 기간별 통계까지 Dashboard의 통계 쿼리 전체(`countTotalTasks`, `countDoneTasks`, `countByStatus`, `countByCategory`, `countCompletionTrend`)를 `DUE_DATE`(마감일) 기준으로 통일했습니다. Oracle `TO_DATE`/`TO_CHAR`를 활용한 기간 검색·날짜 그룹핑 로직도 함께 수정하여 Dashboard/Reports의 집계 기준을 일치시켰습니다.

### 결과

Dashboard 전체 통계가 하나의 기준(`DUE_DATE`)으로 통일되어, 화면마다 기준이 달라 생기던 지표 불일치 문제를 해소하고 통계의 일관성을 확보했습니다.

### 설계 포인트

단순 버그 수정이 아니라 요구사항을 재검토해 통계 기준 자체를 재설계한 사례입니다. 데이터가 언제 생성됐는지보다 업무가 언제까지 마감인지가 사용자의 실제 의사결정에 더 중요하다고 판단하여 기준을 전환했습니다.

---

## 3. 마감 준수율 0%, 평균 완료 소요일 0.0일 버그

### 문제

마감 준수율, 평균 완료 소요일 등 시간 기반 분석 지표가 모두 0으로 집계되는 문제가 있었습니다.

### 원인

완료 시점 계산 로직은 `TASK_LOG`의 `DONE` 상태 변경 이력을 기준으로 동작하도록 설계되어 있었습니다. 그런데 초기 데이터를 생성하는 `DemoDataSeeder`가 `TASK`만 `DONE` 상태로 생성하고 `TASK_LOG`에는 완료 이력을 남기지 않아, 완료 시점(`completedDate`)을 계산할 데이터 자체가 없었습니다.

### 해결

`DemoDataSeeder`에서 `TASK`를 `DONE`으로 생성할 때 `TASK_LOG`에도 함께 완료 이력을 생성하도록 수정하고, `TASK_LOG`의 `DONE` 전환 시점(`MAX(CREATED_DATE)`)을 실제 완료일로 계산해 시간 기반 통계가 동일한 기준을 사용하도록 개선했습니다.

### 결과

평균 완료 소요일과 마감 준수율이 정상적으로 계산되어 시간 분석 통계의 신뢰성을 확보했습니다.

### 배운 점

통계 로직 자체뿐 아니라 테스트(시드) 데이터도 실제 운영 데이터와 동일한 구조를 가져야 분석 결과의 정확성이 보장된다는 점을 경험했습니다.
